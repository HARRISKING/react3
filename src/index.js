import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import './index.css';

ReactDOM.render(
  <h1>我是第一个标签</h1>,
  document.getElementById('root')
);
ReactDOM.render(
  <h2>在react中插入两个便签，实验结果如何</h2>,
  document.getElementById('root1')
)
